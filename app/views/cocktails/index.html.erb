<h1>Index</h1>
<% @cocktails.each do |cocktail| %>
<%= link_to cocktail.name, cocktail_path(cocktail)  %>
<%= button_to "Delete", cocktail_path(cocktail), method: :delete, data: { confirm: 'Are you sure?', disable_with: 'deleting...' } %> <br>

<br>
<% end %>
<%= link_to "Add", new_cocktail_path %>
